# Maintainer: Dauliac <juliendauliac g-mail> 
# Contributors: Dauliac TonyChG

pkgname=youscrap-git
_pkgname=${pkgname%-git}
pkgver=0.5.0
pkgrel=1
pkgdesc="Command line interface for listing website root from dns server name."
arch=('any')
url="https://github.com/TonyChG/$_pkgname"
license=('apache')
depends=('nodejs')
makedepends=('git' 'npm')
install=$pkgname.install
_channel=stable
source=("$_pkgname"::"git+https://github.com/TonyChG/$_pkgname.git")
md5sums=('SKIP')
package() {
    #TODO (Install Script and actions)
}
